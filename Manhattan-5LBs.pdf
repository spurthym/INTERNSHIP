import random

class dino:
    name=str(input("enter your name:"))
    print("we are adding numbers")
    x=random.randint(0, 501)
    y=random.randint(0, 501)
    total=x+y
    ans=int(input("what is "+str(x)+" + " +str(y)+":"))
    def adding(self):
        if(self.ans==self.total):
            print("yes!you are correct!!")
        else:
            print("No your wrong.The right ans is : "+str(self.total))
        
d=dino()
d.adding()